# Workspace — remains local; track only specific subdirectories
workspace/

# Symlinks into the workspace tree
school/
school

# Agent / local tooling (not product source)
.claude-progress.md
.codex
.codex/
corpus/
*-local-command-*.txt

# Build / cache / runtime artifacts
node_modules/
__pycache__/
*.pyc
.venv/
*.log

# Editor / OS / local tooling
.DS_Store
.vscode/
.idea/
.cursor/
**/.cursor/

# Downloaded installers (never commit)
*.deb

# Backup files
*.save

# python-prototype is a nested git repo managed as a submodule by the parent gruff repo
python-prototype/
