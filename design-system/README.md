# Gruff Design System

The local design system ships the token source used by the Gruff CLI and the LO7 notebook surfaces.

- `colors_and_type.css` re-exports the preview token sheet for bootstrap flows.
- `tokens.json` is the machine-readable token contract.
- `assets/` holds reference renders used by compass and signal-chain docs.
