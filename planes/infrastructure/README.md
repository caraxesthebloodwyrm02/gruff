# infrastructure

Ops, hardening, deployment scripts. Empty scaffold awaiting content.

The Hogsmade Audit recommended an `infrastructure/` plane adjacent to services/runs/artifacts/surfaces. Candidates for inclusion as they become first-class:

- `mangrove-hardening` (currently under Hogwarts/nuke adjacencies)
- GRID ops scripts from `Projects/GRID-main/alembic/` and deployment surfaces
- Vision ops scripts
- Workspace-level maintenance scripts from `CascadeProjects/scripts/`

Populate by symlinking from `../../CascadeProjects/...` as you promote scripts to plane-level visibility.
