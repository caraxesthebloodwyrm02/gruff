name: governance
intent: |
  enforce the 6-stage gated execution protocol
  (orient, route, gate, implement, reconcile, report)
  and surface violations or drift.
trigger: manual
timeout: 10m
retry_policy:
  max_attempts: 2
  backoff: exponential
  initial_delay_ms: 1000
owner: prince
lock_key: routine:governance
dispatches: []
produces: []
status: draft
references:
  - ../../../design-system/assets/routine_governance.png
  - ../../../docs/WORKSPACE_GATES.md
