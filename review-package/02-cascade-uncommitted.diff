diff --git a/.windsurf/workflows/startup.md b/.windsurf/workflows/startup.md
index 6a4a463..5fa7eef 100644
--- a/.windsurf/workflows/startup.md
+++ b/.windsurf/workflows/startup.md
@@ -44,7 +44,7 @@ If any are `<NOT SET>`, check `mcp_config.json` — they are injected per-server
 // turbo 3. Verify the GRID venv and sync state:
 
 ```bash
-python_bin="/home/caraxes/CascadeProjects/Projects/GRID-main/.venv/bin/python"
+python_bin="/home/irfankabir/workspace/CascadeProjects/Projects/GRID-main/.venv/bin/python"
 if [ -x "$python_bin" ]; then
   echo "venv OK: $($python_bin --version)"
 else
@@ -55,7 +55,7 @@ fi
 **Fix if missing** (requires approval):
 
 ```bash
-cd /home/caraxes/CascadeProjects/Projects/GRID-main && uv sync --group dev --group test
+cd /home/irfankabir/workspace/CascadeProjects/Projects/GRID-main && uv sync --group dev --group test
 ```
 
 ---
@@ -67,7 +67,7 @@ Required by: `grid-server` (`admission_*` tools, `check_permission`, `health_che
 4. If Area 1 shows GRID API as DOWN, start the mothership (non-blocking, background):
 
 ```bash
-cd /home/caraxes/CascadeProjects/Projects/GRID-main
+cd /home/irfankabir/workspace/CascadeProjects/Projects/GRID-main
 GRID_API_URL=http://localhost:8080 nohup uv run python -m application.mothership.main > /tmp/grid-mothership.log 2>&1 &
 echo "Started PID $! — tailing log for 5s..."
 sleep 5 && tail -20 /tmp/grid-mothership.log
@@ -83,7 +83,13 @@ curl -s --max-time 5 http://localhost:8080/health
 
 ## Area 5 — Ollama health (localhost:11434)
 
-Required by: `grid-rag`, `grid-rag-enhanced` (embedding model `nomic-embed-text-v2-moe:latest`, LLM `ministral-3:latest`).
+Required by: `grid-rag`, `grid-rag-enhanced`.
+
+**Model alignment** (see `Projects/GRID-main/config/ollama-model-catalog.json` for the authoritative, docs-segmented catalog):
+
+* Default embedding model: `nomic-embed-text-v2-moe:latest` (legacy default; can be overridden via `RAG_EMBEDDING_MODEL`). Current-gen alternatives: `embeddinggemma`, `qwen3-embedding`, `all-minilm`.
+* Default LLM: `ministral-3:latest` (legacy default; can be overridden via `RAG_LLM_MODEL_LOCAL`). When `RAG_LLM_MODE=auto` and `RAG_LLM_AUTO_SELECT=catalog`, the catalog's auto-selector picks the best model for the requested task.
+* Current workstation's local Ollama library at time of writing: `gemma4:latest`, `qwen3-coder-next:cloud`. Pulls of the legacy defaults require external network (blocked under UNPROVISIONED MODE).
 
 5. If Area 1 shows Ollama as DOWN, start it:
 
@@ -95,14 +101,20 @@ sleep 3 && curl -s http://localhost:11434/api/tags | python3 -c "import sys,json
 Verify required models are pulled:
 
 ```bash
-ollama list | grep -E "nomic-embed-text|ministral"
+ollama list | grep -E "nomic-embed-text|ministral|embeddinggemma|qwen3|gemma4|gpt-oss"
 ```
 
-If missing:
+If missing (external network required, blocked under UNPROVISIONED):
 
 ```bash
+# Legacy preferred defaults
 ollama pull nomic-embed-text-v2-moe:latest
 ollama pull ministral-3:latest
+
+# Or any current-gen models the catalog knows about:
+ollama pull embeddinggemma            # embedder
+ollama pull qwen3                     # chat + thinking + tools
+ollama pull gpt-oss                   # chat + thinking (levels: low|medium|high)
 ```
 
 ---
@@ -113,7 +125,7 @@ ollama pull ministral-3:latest
 
 ```bash
 for dir in apiguard Vision assistive-agreement-contracts; do
-  path=$(find /home/caraxes/CascadeProjects /home/caraxes/canopy /home/caraxes/grove -maxdepth 3 -type d -name "$dir" 2>/dev/null | head -1)
+  path=$(find /home/irfankabir/workspace/CascadeProjects /home/irfankabir/canopy /home/irfankabir/grove -maxdepth 3 -type d -name "$dir" 2>/dev/null | head -1)
   if [ -n "$path" ]; then
     git -C "$path" rev-parse --git-dir &>/dev/null && echo "$dir: git OK" || echo "$dir: NO GIT REPO at $path"
   else
diff --git a/CLAUDE.md b/CLAUDE.md
index aaa4cdb..873676c 100644
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -210,3 +210,7 @@ python scripts/glimpse-bench.py leaderboard
 - Change **generators and source**, not hand-edited **`dist/`** or lockfiles, unless the task is explicitly to update those files.
 - **Secrets:** Never commit credentials. If found tracked or staged, stop and escalate: **`.gitignore`**, **`git rm --cached`**, and rotation / history scrub are **human-gated** when pushes occurred.
 - **New repos:** `~/seed/templates/gitignore-node-strict.template` or `gitignore-python-uv.template`. **Audit:** `~/scripts/gitignore-audit.sh`.
+
+## Session Log
+
+2026-04-20 — Landed: command-bus + anticipation engine (shared-types, ori-server), DIO governance, GATE archive, viz experiment, submodule bump to 2cc792a, workspace design docs + gitignore. Deferred: echoes signal_automation (3 backlog failures: template.replace type error x2, security_probe classifier gap). Open debt: TS6133/6192 in afloat/echoes/ori src/ — backlog; stub thresholds — won't-fix.
diff --git a/Components/shared-types/src/audit-client.ts b/Components/shared-types/src/audit-client.ts
index 6c1560f..48223d3 100644
--- a/Components/shared-types/src/audit-client.ts
+++ b/Components/shared-types/src/audit-client.ts
@@ -10,6 +10,7 @@ const ECHOES_AUDIT_PATH =
   process.env.ECHOES_AUDIT_PATH || resolve(homedir(), ".echoes", "audit.ndjson");
 
 let dirEnsured = false;
+const MAX_QUEUE_SIZE = 500;
 let writeQueue: Array<{ event: string; resolve: (value: boolean) => void }> = [];
 let isWriting = false;
 
@@ -70,7 +71,6 @@ async function processWriteQueue(): Promise<void> {
       // Lock timeout — skip this write
       resolve(false);
       isWriting = false;
-      setTimeout(processWriteQueue, 0);
       return;
     }
 
@@ -99,8 +99,10 @@ async function processWriteQueue(): Promise<void> {
     resolve(false);
   } finally {
     isWriting = false;
-    // Process next item in queue
-    setTimeout(processWriteQueue, 0);
+    // Process next item in queue only if not already processing
+    if (!isWriting && writeQueue.length > 0) {
+      setTimeout(processWriteQueue, 0);
+    }
   }
 }
 
diff --git a/Tools/MCPServers/ori-server/src/anticipation.ts b/Tools/MCPServers/ori-server/src/anticipation.ts
index 8abd8a3..69b050e 100644
--- a/Tools/MCPServers/ori-server/src/anticipation.ts
+++ b/Tools/MCPServers/ori-server/src/anticipation.ts
@@ -49,6 +49,7 @@ export interface AnticipationSignal {
   action: string;
   generatedAt: string;
   expectedAt?: string;
+  resolvedAt?: string;
   resolved: boolean;
 }
 
@@ -87,10 +88,22 @@ const RISK_THRESHOLD_MEDIUM = 5;
 const RISK_THRESHOLD_HIGH = 10;
 const SIGNAL_TTL_DAYS = 7;
 
+// Schema migration path — old versions map to new fields
+const SCHEMA_MIGRATIONS: Record<string, (store: AnticipationStore) => AnticipationStore> = {
+  "0.9.0": (store) => {
+    // Migrate from 0.9.0: add missing metadata fields
+    store.metadata.totalSignalsGenerated = store.metadata.totalSignalsGenerated || 0;
+    store.metadata.totalSignalsResolved = store.metadata.totalSignalsResolved || 0;
+    store.metadata.activeRiskClusters = store.metadata.activeRiskClusters || 0;
+    return store;
+  },
+};
+
 // ── Store Implementation ──
 
 class PipelineAnticipationStore {
   private path: string;
+  private mutex: Promise<void> = Promise.resolve();
 
   constructor() {
     this.path = path.join(config.dataDir, "anticipation", "store.json");
@@ -102,14 +115,27 @@ class PipelineAnticipationStore {
       const content = await fs.readFile(this.path, "utf-8");
       const store = JSON.parse(content) as AnticipationStore;
 
-      // Validate schema version
-      if (store.metadata.schemaVersion !== ANTICIPATION_SCHEMA_VERSION) {
+      // Check schema version and migrate if needed
+      const storeVersion = store.metadata.schemaVersion;
+      if (storeVersion !== ANTICIPATION_SCHEMA_VERSION) {
+        // Try migration path first
+        if (SCHEMA_MIGRATIONS[storeVersion]) {
+          const migrated = SCHEMA_MIGRATIONS[storeVersion](store);
+          migrated.metadata.schemaVersion = ANTICIPATION_SCHEMA_VERSION;
+          await this.save(migrated);
+          return migrated;
+        }
+        // No migration path — warn and create new store
+        process.stderr.write(
+          `[anticipation] schema version mismatch: ${storeVersion} -> ${ANTICIPATION_SCHEMA_VERSION}. ` +
+            "Active signals will be discarded. No migration path available.\n",
+        );
         return this.createDefaultStore();
       }
 
       // Prune resolved signals older than TTL
       const ttlCutoff = new Date(Date.now() - SIGNAL_TTL_DAYS * 24 * 60 * 60 * 1000).toISOString();
-      store.resolvedSignals = store.resolvedSignals.filter((s) => s.generatedAt >= ttlCutoff);
+      store.resolvedSignals = store.resolvedSignals.filter((s) => (s.resolvedAt ?? s.generatedAt) >= ttlCutoff);
 
       return store;
     } catch {
@@ -141,30 +167,37 @@ class PipelineAnticipationStore {
   }
 
   async appendSignal(signal: AnticipationSignal): Promise<void> {
-    const store = await this.load();
-    store.activeSignals.push(signal);
-    store.metadata.totalSignalsGenerated++;
-    store.metadata.lastUpdated = new Date().toISOString();
-    await this.save(store);
+    this.mutex = this.mutex.then(async () => {
+      const store = await this.load();
+      store.activeSignals.push(signal);
+      store.metadata.totalSignalsGenerated++;
+      store.metadata.lastUpdated = new Date().toISOString();
+      await this.save(store);
+    });
+    await this.mutex;
   }
 
-  async resolveSignal(signalId: string, outcome: string): Promise<void> {
-    const store = await this.load();
-    const signalIndex = store.activeSignals.findIndex((s) => s.id === signalId);
+  async resolveSignal(signalId: string, _outcome: string): Promise<void> {
+    this.mutex = this.mutex.then(async () => {
+      const store = await this.load();
+      const signalIndex = store.activeSignals.findIndex((s) => s.id === signalId);
 
-    if (signalIndex !== -1) {
-      const signal = store.activeSignals[signalIndex];
-      signal.resolved = true;
+      if (signalIndex !== -1) {
+        const signal = store.activeSignals[signalIndex];
+        signal.resolved = true;
+        signal.resolvedAt = new Date().toISOString();
 
-      // Move from active to resolved
-      store.activeSignals.splice(signalIndex, 1);
-      store.resolvedSignals.push(signal);
+        // Move from active to resolved
+        store.activeSignals.splice(signalIndex, 1);
+        store.resolvedSignals.push(signal);
 
-      store.metadata.totalSignalsResolved++;
-      store.metadata.lastUpdated = new Date().toISOString();
+        store.metadata.totalSignalsResolved++;
+        store.metadata.lastUpdated = new Date().toISOString();
 
-      await this.save(store);
-    }
+        await this.save(store);
+      }
+    });
+    await this.mutex;
   }
 
   async getActiveSignals(): Promise<AnticipationSignal[]> {
@@ -221,7 +254,8 @@ export class PipelineAnticipationEngine {
   async analyzeRecentLogs(logs: LogEntry[]): Promise<AnticipationSignal[]> {
     if (logs.length === 0) return [];
 
-    const probe = runProbe(logs, "anticipation-engine");
+    // Run probe for side effects (logging, metrics) if needed
+    runProbe(logs, "anticipation-engine");
     const registry = await loadRegistry();
     const signals: AnticipationSignal[] = [];
 
@@ -243,7 +277,8 @@ export class PipelineAnticipationEngine {
       if (entry.severity === "critical") projectStats.critical++;
       if (entry.severity === "warning") projectStats.warning++;
 
-      entry.matchedPatterns.forEach((pattern) => projectStats.patterns.add(pattern));
+      const stats = projectStats;
+      entry.matchedPatterns.forEach((pattern) => stats.patterns.add(pattern));
     }
 
     // Generate signals for each project with significant risk
