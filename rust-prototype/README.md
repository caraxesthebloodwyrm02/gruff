# Rust Prototype

Minimal scaffolding for Rust prototyping.

This runtime remains a healthy secondary prototype. The canonical LO7 notebook implementation lives in [`python-prototype`](/home/irfankabir/gruff/workspace/python-prototype), while Rust currently preserves the grid and notebook baseline only.

## Run
```bash
cd rust-prototype
cargo run
```

## Test
```bash
cargo test
```
