# Gruff Init Template

This directory was initialized by `gruff init`.

- `tokens.css` and `tokens.json` provide GRID design tokens.
- `gruff-proportion-v1.schema.json` is the bridge contract for wall-board style snapshots.
- `voice.md` documents the visual language exported from the local design system.
